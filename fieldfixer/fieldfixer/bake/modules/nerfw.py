"""NeRF-W wrapper stub."""

from __future__ import annotations

from pathlib import Path
from typing import Any


def run_nerfw(config: dict[str, Any], workspace: Path) -> None:
    """Train NeRF-W, fit appearance embeddings, and emit LUT transforms."""

    raise NotImplementedError("NeRF-W integration pending")
